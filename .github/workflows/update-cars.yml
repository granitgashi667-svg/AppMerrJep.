name: Update Car Data

on:
  schedule:
    - cron: '0 3 * * *'   # Çdo ditë në 03:00 UTC
  workflow_dispatch:        # Ose manualisht

jobs:
  fetch-cars:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install encar
        run: pip install encar

      - name: Fetch car data
        run: python fetch_cars.py

      - name: Commit and push
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'Automated: Update car data'
          file_pattern: 'cars.json'
